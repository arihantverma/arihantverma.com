---
layout: layouts/home.njk
eleventyNavigation:
  key: Home
  order: 1
---

{% set maxPosts = collections.posts.length | min(10) %}
<h1 class="post__heading font-family-system font-family-override-impact font-family-helvetica d-font-family-impact color-dark-gray">
  Most Recent
  {% if maxPosts == 1 %}
    Post
  {% else %}
    {{ maxPosts }} Posts
  {% endif %}
</h1>

{% set postslist = collections.posts | head(-10) %}
{% set postslistCounter = collections.posts | length %}
{% include "postslist.njk" %}


<p class="font-family-etbook font-size-xlarge">More posts can be found in <a href="{{ '/posts/' | url }}" class="site-common-link">the archive</a>.</p>
